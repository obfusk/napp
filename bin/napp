#!/usr/bin/env ruby

unless Gem.loaded_specs.include? 'napp'
  $:.unshift File.expand_path('../../lib', __FILE__)
end

require 'napp'
require 'napp/cmd'

puts "napp v#{Napp::VERSION}"
Napp::Cmd.run *ARGV
